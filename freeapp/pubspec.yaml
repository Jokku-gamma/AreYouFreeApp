# pubspec.yaml
name: event_app
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Adjust based on your Flutter SDK version

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase dependencies - Force to specific latest versions to ensure webClientId support
  firebase_core: ^3.15.1 # Try this version or check pub.dev for the absolute latest stable
  firebase_auth: ^5.6.2 
  cloud_firestore: ^5.6.11 
  google_sign_in: ^7.1.0 

  # State management
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
